{
  "require": ["ts-node/register/transpile-only", "./test/mocha/setup.ts"],
  "extension": ["ts"],
  "spec": "test/mocha/**/*.test.ts",
  "recursive": true,
  "timeout": 10000,
  "reporter": "spec"
}